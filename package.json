{
  "name": "maslph",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp og-image.png public/"
  },
  "dependencies": {
    "@vercel/og": "latest"
  }
}
